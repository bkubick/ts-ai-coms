/**
 * @fileoverview index of models for the AI module.
 * 
 * This file exports all of the models for the AI module. This is done so that
 * the models can be imported from a single file, rather than having to import
 * each model individually.
 */

import { ChatGPT, GPTModel } from './chatgpt.model';


export { ChatGPT, GPTModel };
